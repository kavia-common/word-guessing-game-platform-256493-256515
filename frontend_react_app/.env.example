# Environment variables for the React frontend
# Note: Create a .env file and copy the keys you want to override.

# API base URL (must include /api suffix). Examples:
# http://localhost:3001/api
# https://your-backend.example.com/api
REACT_APP_API_BASE=http://localhost:3001/api
