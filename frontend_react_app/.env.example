# Base URL for the backend API (no trailing slash), e.g.:
# http://localhost:3001
# https://your-preview-host:3001
REACT_APP_API_BASE=

# Optional site URL for redirects in auth flows (if applicable)
REACT_APP_SITE_URL=
