# Build-time environment variables for CRA (react-scripts)
# Note: For deployments, prefer runtime override via public/runtime-config.js

# Backend API base (include /api). Can also be set at runtime via window.__API_BASE__.
REACT_APP_API_BASE=http://localhost:3001/api

# Supabase configuration (public anon key only, never service role keys here)
REACT_APP_SUPABASE_URL=
REACT_APP_SUPABASE_ANON_KEY=

# Frontend site URL used for Supabase email redirect (optional)
REACT_APP_SITE_URL=http://localhost:3000
