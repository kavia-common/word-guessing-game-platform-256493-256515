# Frontend environment variables (Create React App)
# Preferred approach is to use public/runtime-config.js to set window.__API_BASE__ at runtime.
# If you need to set at build-time, uncomment and set REACT_APP_API_BASE to the exact backend origin including /api.
# REACT_APP_API_BASE=https://your-backend-host:3001/api

# Optional Supabase auth config (build-time). Prefer runtime-config.js instead.
# REACT_APP_SUPABASE_URL=
# REACT_APP_SUPABASE_ANON_KEY=
# REACT_APP_SITE_URL=https://your-frontend-origin
