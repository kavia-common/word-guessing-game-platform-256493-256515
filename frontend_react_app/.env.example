# Example environment configuration for the React frontend (CRA).
# Copy to ".env" and adjust values for your environment.

# PUBLIC_INTERFACE
# Effective API base URL used by src/api/client.js
# Must include the /api path segment (e.g., http://localhost:3001/api)
REACT_APP_API_BASE=http://localhost:3001/api

# Optional: The publicly accessible frontend URL (used by ops/docs; not read by code here)
REACT_APP_FRONTEND_URL=http://localhost:3000

# Optional: If using websockets elsewhere in your app
REACT_APP_WS_URL=

# Environment toggles
REACT_APP_NODE_ENV=development
REACT_APP_NEXT_TELEMETRY_DISABLED=1
REACT_APP_ENABLE_SOURCE_MAPS=true
REACT_APP_PORT=3000
REACT_APP_TRUST_PROXY=false
REACT_APP_LOG_LEVEL=info
REACT_APP_HEALTHCHECK_PATH=/health
REACT_APP_FEATURE_FLAGS=
REACT_APP_EXPERIMENTS_ENABLED=false

# Note:
# - You can override at runtime without rebuild by defining BEFORE the bundle loads:
#     <script>window.__API_BASE__ = "https://your-backend-host:3001/api";</script>
#   This takes precedence over REACT_APP_API_BASE.
